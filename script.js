const heading = document.createElement('h2');
const headingTagls = document.createElement('h2');
document.body.style.textAlign = 'center';
document.body.style.backgroundColor = 'white';
document.body.style.color = 'Orange';
document.body.style.marginTop = '300px';
document.body.style.fontSize = "x-large";
document.body.append(heading);
document.body.append(headingTagls);
var countDown = 10;
setTimeout(() => {
    heading.innerText = '';
    heading.innerText = countDown;
    countDown--;
    setTimeout(() => {
        heading.innerText = '';
        heading.innerText = countDown;
        countDown--;
        setTimeout(() => {
            heading.innerText = '';
            heading.innerText = countDown;
            countDown--;
            setTimeout(() => {
                heading.innerText = '';
                heading.innerText = countDown;
                countDown--;
                    setTimeout(() => {
                    heading.innerText = '';
                    heading.innerText = countDown;
                    countDown--;
                        setTimeout(() => {
                        heading.innerText = '';
                        heading.innerText = countDown;
                        countDown--;
                        setTimeout(() => {
                            heading.innerText = '';
                            heading.innerText = countDown;
                            countDown--;
                            setTimeout(() => {
                            heading.innerText = '';
                            heading.innerText = countDown;
                            countDown--;
                            setTimeout(() => {
                                heading.innerText = '';
                                heading.innerText = countDown;
                                countDown--;
                                setTimeout(() => {
                                    heading.innerText = '';
                                    heading.innerText = countDown;
                                    countDown--;
                                    setTimeout(() => {
                                        heading.innerText = '';
                                        headingTagls.innerText = 'Happy Independence Day!';
                                    countDown--;
                                }, 900);
                            }, 900);
                            }, 900);
                        }, 900);
                        }, 900);
                    }, 900);
                }, 900);
            }, 900);
        }, 900);
    }, 900);
}, 900);

